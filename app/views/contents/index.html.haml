.span-2 &nbsp;
.span-20

  %h1 CMS:

  %table
    %tr
      %th Title
      %th Body
      %th Context
      %th
      %th
      %th

    - @contents.each do |content|
      %tr
        %td= content.title
        %td= content.body
        %td= content.context
        %td= link_to 'Show', content
        %td= link_to 'Edit', edit_content_path(content)
        %td= link_to 'Destroy', content, :confirm => 'Are you sure?', :method => :delete

  %br

  = link_to 'New CMS Item', new_content_path

.span-2.last &nbsp;